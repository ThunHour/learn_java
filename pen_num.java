import java.util.Scanner;
public class pen_num {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        String numdigit=scanner.nextLine();
        int change=Integer.parseInt(numdigit);
        
    }
}
